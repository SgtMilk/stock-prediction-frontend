import { CSSProperties } from "react";

export const objectCSS: { [key: string]: CSSProperties } = {
  inlineFlex: {
    flex: "inline-flex",
    flexDirection: "row",
  },
  padding: {
    width: "0.5rem",
  },
};
